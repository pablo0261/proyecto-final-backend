const {
    People,
    People_options,
    Categories,
    Categories_options,
    People_logins,
    Opportunities,
    Payments,
} = require("../../db.js");

const getCustomerStatsService = async () => {
    
    try {
        // code
    } catch (error) {
        console.log("CustomerStats: ", error);
        throw error;
    }
}

module.exports = { getCustomerStatsService };